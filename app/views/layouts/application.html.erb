<!DOCTYPE html>
<html>
  <head>
    <title>Flashcards</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <h1>Flashcards</h1>
    <div><%= link_to 'Decks', controller: 'deck', action: 'index' %></div>
    <div><%= link_to 'New Deck', controller: 'deck', action: 'new' %></div>
    <%= yield %>
  </body>
</html>
